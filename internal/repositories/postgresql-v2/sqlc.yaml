version: "2"
sql:
  - engine: "postgresql"
    queries: "./queries"
    schema: "./migrations"
    gen:
      go:
        package: "gen"
        out: "./gen"
        emit_empty_slices: true
        emit_prepared_queries: true